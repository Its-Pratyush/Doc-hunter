services:
  surrealdb:
    image: surrealdb/surrealdb:latest
    user: "0:0"   
    ports:
      - "8000:8000"
    command: start --log debug --user root --pass root surrealkv:///my-data
    volumes:
      - surreal_data:/my-data

volumes:
  surreal_data: